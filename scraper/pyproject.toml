[project]
name = "harmonyos-docs"
version = "0.1.0"
description = "Local mirror of HarmonyOS developer docs as Markdown for AI-assisted HarmonyOS app development"
requires-python = ">=3.12"
dependencies = [
    "markdownify>=0.13",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
    "pyyaml>=6.0",
    "httpx>=0.27",
]

# dev 组由 uv sync / uv run 默认安装（PEP 735），不必再手动 pip install ".[dev]"
[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["scripts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
